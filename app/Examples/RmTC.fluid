data Bool : Type where
  True  : Bool
  False : Bool

eq : Nat -> Nat -> Bool
eq Z Z = True
eq Z (S _) = False
eq (S _) Z = False
eq (S x) (S y) = eq x y

f : Nat -> Nat
f x = case (eq x x) of
  | True => Z
  | False => (S Z)
